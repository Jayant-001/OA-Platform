#include <bits/stdc++.h>
using namespace std;

int main() 
{
    long n = 100000, sum = 0;
    // int arr[n];
    // memset(arr, -1, sizeof(arr));

    for(int i = 0; i < n; i++) {
        for(int j = 0; j < n; j++) {
            sum += 1LL;
        }
   }
   cout << sum << endl;

    cout << "Hello, " << endl;
    return 0;
} 